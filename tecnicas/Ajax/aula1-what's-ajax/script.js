/* 

# O que é o Ajax?

Ajax (Asynchronous JavaScript and XML) é uma técnica de desenvolvimento web que permite a atualização assíncrona de partes de uma página da web, sem a necessidade de recarregar a página inteira. Isso é feito através do uso de JavaScript para enviar e receber dados do servidor em segundo plano.

Isso resulta em uma experiência de usuário mais fluida e interativa, pois permite que o conteúdo da página seja atualizado dinamicamente com base nas ações do usuário ou em eventos específicos.
# Como o Ajax funciona?

O Ajax funciona através do objeto XMLHttpRequest (XHR) ou da API Fetch, que permite que o JavaScript envie solicitações HTTP para o servidor e receba respostas. O processo básico envolve os seguintes passos:

1. O usuário interage com a página (por exemplo, clicando em um botão).

2. O JavaScript cria uma solicitação HTTP assíncrona usando o objeto XMLHttpRequest ou a API Fetch.

3. A solicitação é enviada ao servidor.

4. O servidor processa a solicitação e envia uma resposta (geralmente em formato JSON, XML ou HTML).

5. O JavaScript recebe a resposta e atualiza a página dinamicamente com os novos dados, sem recarregar a página inteira.

# Vantagens do Ajax

- Melhor experiência do usuário: Atualizações parciais da página tornam a navegação mais rápida e fluida.

- Redução do tráfego de dados: Apenas os dados necessários são enviados e recebidos, economizando largura de banda.

- Interatividade: Permite a criação de aplicações web mais interativas e responsivas.

*/

 